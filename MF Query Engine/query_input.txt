SELECT ATTRIBUTE(S):
prod, month, 1_avg_quant
NUMBER OF GROUPING VARIABLES(n):
1
GROUPING ATTRIBUTES(V):
prod, month
F-VECT([F]):
1_avg_quant
SELECT CONDITION-VECT([σ]):
1.prod == prod and 1.month >= month - 3 and 1.month <= month + 1
HAVING_CONDITION(G):